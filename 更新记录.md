# 更新记录

桑葚集 (Mulberry) — 个人收藏集公开站

## v0.3.0 - 2026-07-30

- 顶栏“资源”改为下拉菜单:hover 展开「Skill」「文章」两个选项(主题无关深色,锁 -always token)。
- 新增文章列表页 `/words`:风格适配自 getdesign.md 的 Blog 页,用 Mulberry 紫替代粉色调，与 FindSkills 表格风格统一。
- 新增文章详情页 `/word/:slug`:面包屑 + 标题 + 副标题 + 元信息 + 左侧 760px 正文 + 右侧 220px「ON THIS PAGE」锚点侧边栏(与现有 detail.css 的 md-body 风格一致)。
- 新增数据脚本 `scripts/build-articles.mjs`:从 Mulberry-word 仓库读 `<cat>/<slug>/README.md` 生成 `public/articles.json`;构建时提取 h2/h3 为 toc 字段。
- 新增 `useArticles` hook + `/api/word/*` Vercel 代理(5 分钟边缘缓存);与 Skill 体系平行。
- 新增 `categories.json` 未初始化时退化为空仓库,不回退到 GitHub 避免 409 限流。
- readme 仓库结构约定: `<cat>/<slug>/README.md`,frontmatter 字段:title/desc/date/tag/author/readingTime;slug 全局唯一。

## v0.2.2 - 2026-07-30

- 修：浅色模式下首页“这是桑葚的收藏集”文字变深色的问题。
  - 原因：TextType 用 `textColors` 内联 style 覆盖了 `.hero-headline` 的颜色。
  - 修复：textColors 的第一项 `var(--fg)` 改为 `var(--fg-always)`，保持始终浅色。

## v0.2.1 - 2026-07-30

- 主题切换从三态（system/light/dark）简化为两态（light/dark），去掉跟随系统功能，默认深色。
- 顶栏（Navigation）始终保持深色模式下的样子，light 模式下也不变 — 通过 `-always` token 锁定。
- 首页 Hero 区（背景/文字/按钮）始终保持深色模式下的样子，light 模式下也不变。
- Hero FaultyTerminal 动画色不再随主题切换，始终 Mulberry 紫。About Dither 依然跟随主题。

## v0.2.0 - 2026-07-30

- 新增主题切换：三态（system / light / dark），默认跟随系统。
- 导航栏右侧加主题切换图标按钮，循环切换三态；状态持久化到 localStorage。
- `index.html` 加防闪烁内联脚本，首屏不闪深色。
- 新增 `src/context/ThemeContext.jsx`、`src/components/ThemeToggle.jsx`、`src/config/animation.js`。
- 动画配置集中到 `src/config/animation.js`：Hero FaultyTerminal 与 About Dither 都接入 hook；亮色模式下动画色自动改为白色。
-  颜色 token 全面变量化（`index.css` 拆分出 hover-bg / btn-primary / hero-overlay / scrollbar 等 token），同时加上 `:root[data-theme="light"]` 浅色模式覆盖。
- 修所有 hardcode 颜色：hero.css、navigation.css、find-skills.css、detail.css 改为 `var(--token)`。

## v0.1.9 - 2026-07-30

- 撤销 v0.1.8 为关于页添加的 SplitText 动画，保留用户手动改的文案与 Dither 参数。

## v0.1.8 - 2026-07-30

- 关于页正文接入 GSAP SplitText 字符入场动画（`opacity 0→1 + y 30/40→0` staggered）:依次为h1 标题、h2 小标题、p 段落。
- 新增依赖：`@gsap/react`。
- 新增组件：`src/components/SplitText.jsx`（带 `gsap/SplitText` 插件的 React Bits 集成）。

## v0.1.7 - 2026-07-30

- 新增「关于」页面：单屏 50/50 布局，左侧 Dither WebGL 动画(复古 Bayer 抖动 + Mulberry 紫波浪)，右侧站点介绍 + 作者介绍，中间 1px 分割线。
- 新增依赖：three / postprocessing / @react-three/fiber / @react-three/postprocessing。
- 顶部导航「关于」改为路由链接（`/about`）。

## v0.1.6 - 2026-07-30

- Hero 背景 WebGL 加入 IntersectionObserver：滚动到列表时暂停 rAF，回到 Hero 时恢复，GPU 占用只在 Hero 可见时发生。
- 修正 StrictMode 下 useEffect cleanup 未重置 rafRef 导致的本地动画不启动问题。
- 调整 Hero 动画参数（noiseAmp / timeScale / scale / gridMul 等），保证背景动画可持续呈现。
- 首页 Skill 描述统一为中文一句话总结：每个 skill 的 README.md 新增 `desc` 字段，build-index 优先使用该字段。

## v0.1.5 - 2026-07-30

- 修复打开页面后浏览器卡、桌面在页面台前时也卡的性能问题:Hero 区 WebGL 背景渲染参数全面下调(锁 1x dpr、关闭色差与噪声振幅、降低闪烁与故障幅度等),单帧 GPU 负载大幅下降,中低端显卡也能流畅运行。

## v0.1.4 - 2026-07-30

- 修复详情页显示空模板的问题:之前所有 skill 详情页都被错误地套上"设计系统速查"模板,现在恢复为各 skill 自己的原始内容。
- 数据规约从 `README.md + DESIGN.md` 双文件简化为 `SKILL.md` 单文件,直接保留源文件原貌。
- 详情页同步改造,只读一份 `SKILL.md`。
- 上传工作流 `mulberry-publish-skill` 重写,加上"不写设计模板"强制条款与空模板自检闸门,防止类似问题再发生。

## v0.1.3 - 2026-07-30

- 修复首页部署后用户必须手动刷新才能看到新 skill 的问题(`useSkillData.js` 的 fetch 缓存策略由 `force-cache` 改为 `no-cache`,带 ETag revalidate)。

## v0.1.2 - 2026-07-30

- 浏览器页签加 favicon(桑葚图标矢量),标题简化为"桑葚集";顶栏 Logo 旁加 22x22 桑葚图标。
- Hero 板块 WebGL 背景与标题打字机动效微调,降低 GPU 负载。
- 站点内容从 10 个 demo skill 替换为 20 个真实 Qoder skill,涵盖 design / dev-tools / saas 三类。
- 数据仓库 `nothingtosayyy/Mulberry-SKILL` 同步推送,自定义域名启用 `https://mulberrytian.vercel.app/`。
- 新增本地工作流 skill `mulberry-publish-skill`,负责后续新 skill 的上传 + 站点同步。
- `rent-expense-tracker` 按用户要求未上传。

## v0.1.1 - 2026-07-29

- Hero 板块加 WebGL 故障终端背景(`ogl` 实现)与打字机标题动画(`gsap` 实现)。
- 修复 `/api/raw/:path*` 在 Vercel Functions 嵌套 catch-all 上的 404 问题(改用 `vercel.json` 的 `rewrites`)。
- 响应式 hero 高度微调,给 WebGL 背景足够可视区。

## v0.1.0 - 2026-07-29

- 桑葚集首版上线:React + Vite SPA,首页 + 详情页 + 10 个 Skill(Claude / Anthropic / BMW M / ClickHouse / Figma / Cursor / Airbnb / Stripe / Spotify / Linear)。
- 数据源分离:站点静态构建,Skill 详情走 Vercel Edge 代理 GitHub raw,GitHub API 限流隔离。
- 部署到 Vercel,源码仓库 https://github.com/nothingtosayyy/Mulberry-web。
