# 更新记录

桑葚集 (Mulberry) — 个人收藏集公开站

## v0.1.3 - 2026-07-30

- 修复首页部署后用户必须手动刷新才能看到新 skill 的问题。
  - `src/hooks/useSkillData.js`:`fetch('/index.json', { cache: 'force-cache' })` 改为 `cache: 'no-cache'`。
  - 行为:浏览器本地仍缓存,但每次请求带 ETag revalidate;资源未变返回 304(几百字节,几乎零成本),资源变了(部署后)立即拿到新版本。
  - 影响:用户零感知自动获取最新内容,无需 Ctrl+Shift+R。

## v0.1.2 - 2026-07-30

- 浏览器页签加 favicon (public/favicon.svg): 桑葚图标矢量,1024x1024 紫绿配色。
- 浏览器页签标题: "桑葚集 — 个人收藏集" → "桑葚集"。
- 顶栏左侧 Logo 旁加 22x22 桑葚图标 (内联 SVG, src/components/Navigation.jsx)。
- Hero 性能微调 (src/components/Hero.jsx): FaultyTerminal `timeScale 1→0.5`, `noiseAmp 0.8→0.7`, `chromaticAberration 1→0.3`, `dither 1→0`, `mouseStrength 1→0.3`, 关闭 `mouseReact`,降低 shader 计算量。
- 站点内容更新 (public/index.json): 用 20 个 Qoder skill 替换 10 个 demo skill — design (2): ai-page-polish, ui-designer; dev-tools (3): app-version-policy, frontend-design, prototype-factory; saas (15): airtable-design, content-research-writer, deep-research, documents, jwynia-requirements-analysis, natural-writing, pm-brainstorm, pm-competitor, pm-metrics-review, pm-prd, pm-prioritization, pm-roadmap, pm-user-feedback, pm-user-story, product-manager-proactive。
- 数据仓库 `nothingtosayyy/Mulberry-SKILL` 同步推送 20 个新 skill + 删除 10 个 demo (commit 2beed52)。
- 自定义域名: `https://mulberrytian.vercel.app/`。
- 新增项目本地 skill: `.qoder/skills/mulberry-publish-skill/SKILL.md`,负责后续新 skill 的格式化 + 上传 + 站点同步全流程。
- `rent-expense-tracker` 按用户要求未上传。

## v0.1.1 - 2026-07-29

- Hero 板块加 WebGL 故障终端背景:基于 `ogl` 的 `<FaultyTerminal />` 组件,accent 紫色调,#7938eb tint,数字字符 + 扫描线 + 故障动画 + 鼠标交互。
- Hero 标题加打字机动画:基于 `gsap` 的 `<TextType />` 组件,"这是桑葚的收藏集 / 期望这些内容可以帮助到你" 两句话循环打字,白色 + 紫色高亮交替,光标 `_` 闪烁。
- 修复:用 `vercel.json` 的 `rewrites` 把 `/api/raw/:path*` 代理到 `raw.githubusercontent.com`,解决多段路径在 Vercel Functions 嵌套 catch-all 上的 404 问题。
- 响应式 hero 高度调整,1024px 断点 240→360px,768px 断点 200→320px,给 WebGL 背景足够可视区。
- 新增依赖:`ogl` (WebGL 渲染), `gsap` (光标动画)。

## v0.1.0 - 2026-07-29

- 桑葚集初始版本发布:React + Vite SPA,首页 + 详情页 + 10 个 Skill (Claude / Minimax / BMW M / ClickHouse / Figma / Cursor / Airbnb / Stripe / Spotify / Linear)。
- 数据源分离:站点静态构建,Skill 详情走 Vercel Edge 代理 GitHub raw,GitHub API 限流隔离。
- 部署到 Vercel (https://ms5gmexp4ubcf6qt.vercel.app),源码仓库 https://github.com/nothingtosayyy/Mulberry-web。
